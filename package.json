{
  "name": "drizzle-migration-test",
  "type": "module",
  "private": true,
  "scripts": {
    "db:generate": "drizzle-kit generate --name",
    "db:generate:empty": "drizzle-kit generate --name",
    "test": "vitest",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "drizzle-kit": "^1.0.0-rc.4",
    "testcontainers": "^12.0.4",
    "tsx": "^4.23.1",
    "vitest": "^3.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@testcontainers/postgresql": "^12.0.4",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^1.0.0-rc.4",
    "pg": "^8.22.0"
  }
}
